FROM python:3.11-slim

WORKDIR /app
ENV PYTHONPATH=/app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY demo-service /app/demo-service

CMD ["uvicorn", "app:app", "--app-dir", "/app/demo-service", "--host", "0.0.0.0", "--port", "8000"]
